[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gallery"
version = "0.1"
dependencies = [
	"ffmpeg-python",
	"pillow",
	"pillow-heif",
	"exifread",
	"Flask",
	"Flask-SQLAlchemy"
]

[tool.setuptools.packages.find]
where = ["src"]

